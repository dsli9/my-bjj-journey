version: 2

models:
  - name: position_metrics
    columns:
      - name: position
        tests:
          - not_null
        description: Test

      - name: aggregation_category
        tests:
          - not_null

      - name: aggregation_value
      - name: metric_name
      - name: metric_value
